import React,{useState} from "react";import {Plus,Trash2} from "lucide-react";import {useApp} from "../context/AppContext";export default function Page(){const {tasks,createItem,deleteItem}=useApp();const items=tasks.filter(t=>t.type==="automation");const [form,setForm]=useState(null);const start={type:"automation",title:"",target:30,current:0,period:"Monthly",objective:"",start:"",end:"",status:"Planning",hook:"",category:"Travel",priority:"Medium",notes:"",trigger:"Before publish time",action:"Create reminder",enabled:true,body:""};return <div className="page"><div className="page-head"><div><span className="eyebrow">No-code rules</span><h2>Automations</h2></div><button className="primary" onClick={()=>setForm(start)}><Plus size={18}/> Add Rule</button></div><div className="automation-grid">{items.map(i=><article className="automation-card" key={i.id}><span>{i.status||i.priority||i.category||i.period||"Active"}</span><h3>{i.title}</h3><p>{i.objective||i.hook||i.body||i.notes||i.trigger||"Saved item"}</p><small>{i.start&&i.end?`${i.start} → ${i.end}`:i.action||i.period||i.category}</small><button onClick={()=>deleteItem("tasks",i.id)}><Trash2 size={15}/></button></article>)}</div>{!items.length&&<div className="empty">No items yet</div>}{form&&<div className="modal"><form className="modal-card" onSubmit={async e=>{e.preventDefault();await createItem("tasks",form);setForm(null)}}><div className="modal-head"><h3>Add Rule</h3><button type="button" onClick={()=>setForm(null)}>×</button></div><label>Rule Name</label><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><label>Trigger</label><input value={form.trigger} onChange={e=>setForm({...form,trigger:e.target.value})}/><label>Action</label><input value={form.action} onChange={e=>setForm({...form,action:e.target.value})}/><label>Enabled</label><input value={form.enabled} onChange={e=>setForm({...form,enabled:e.target.value})}/><button className="primary wide">Save</button></form></div>}</div>}