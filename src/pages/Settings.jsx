import React from "react";import {useApp} from "../context/AppContext";export default function Settings(){const {user,dark,setDark}=useApp();return <div className="page"><div className="page-head"><div><span className="eyebrow">Workspace preferences</span><h2>Settings</h2></div></div><div className="two-panel"><section className="panel"><h3>Profile</h3><p><b>Name:</b> {user?.displayName}</p><p><b>Email:</b> {user?.email}</p></section><section className="panel"><h3>Appearance</h3><label className="toggle-row"><span>Dark Mode</span><input type="checkbox" checked={dark} onChange={e=>setDark(e.target.checked)}/></label></section></div><section className="panel"><h3>Functional Scope</h3><p>External API ছাড়াই যেগুলো code দিয়ে functional করা সম্ভব, সেগুলো এই build-এ দেওয়া হয়েছে। Auto social posting, payment, strict multi-user claims provider setup ছাড়া করা যায় না।</p></section></div>}