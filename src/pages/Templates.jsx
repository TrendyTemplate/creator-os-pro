import React,{useState} from "react";import {Plus,Trash2} from "lucide-react";import {useApp} from "../context/AppContext";export default function Page(){const {tasks,createItem,deleteItem}=useApp();const items=tasks.filter(t=>t.type==="template");const [form,setForm]=useState(null);const start={type:"template",title:"",target:30,current:0,period:"Monthly",objective:"",start:"",end:"",status:"Planning",hook:"",category:"Travel",priority:"Medium",notes:"",trigger:"Before publish time",action:"Create reminder",enabled:true,body:""};return <div className="page"><div className="page-head"><div><span className="eyebrow">Reusable assets</span><h2>Templates</h2></div><button className="primary" onClick={()=>setForm(start)}><Plus size={18}/> Add Template</button></div><div className="template-grid">{items.map(i=><article className="template-card" key={i.id}><span>{i.status||i.priority||i.category||i.period||"Active"}</span><h3>{i.title}</h3><p>{i.objective||i.hook||i.body||i.notes||i.trigger||"Saved item"}</p><small>{i.start&&i.end?`${i.start} → ${i.end}`:i.action||i.period||i.category}</small><button onClick={()=>deleteItem("tasks",i.id)}><Trash2 size={15}/></button></article>)}</div>{!items.length&&<div className="empty">No items yet</div>}{form&&<div className="modal"><form className="modal-card" onSubmit={async e=>{e.preventDefault();await createItem("tasks",form);setForm(null)}}><div className="modal-head"><h3>Add Template</h3><button type="button" onClick={()=>setForm(null)}>×</button></div><label>Template Title</label><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><label>Category</label><input value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/><label>Body</label><input value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/><button className="primary wide">Save</button></form></div>}</div>}