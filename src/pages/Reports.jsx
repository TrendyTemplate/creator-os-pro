import React from "react";import {useApp} from "../context/AppContext";import {overallStatus} from "../lib/helpers";export default function Reports(){const {allVideos,brands,allMedia,tasks}=useApp();return <div className="page"><div className="page-head"><div><span className="eyebrow">Advanced reports</span><h2>Reports</h2></div></div><section className="stats-grid">{[["Total Content",allVideos.length],["Published",allVideos.filter(v=>overallStatus(v)==="Published").length],["Scheduled",allVideos.filter(v=>overallStatus(v)==="Scheduled").length],["Brands",brands.length],["Media Assets",allMedia.length],["Tasks",tasks.length]].map(([l,v])=><div className="stat-card" key={l}><span>{l}</span><strong>{v}</strong></div>)}</section><section className="panel"><h3>Brand-wise Content</h3>{brands.map(b=>{const n=allVideos.filter(v=>v.brandId===b.id).length;return <div className="metric-row" key={b.id}><span>{b.name}</span><div className="metric-bar"><i style={{width:(allVideos.length?Math.round(n/allVideos.length*100):0)+"%"}}/></div><b>{n}</b></div>})}</section></div>}