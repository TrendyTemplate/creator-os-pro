import React,{useState} from "react";import {CheckCircle2,AlertTriangle,ExternalLink} from "lucide-react";import {useApp} from "../context/AppContext";import PlatformLogo from "../components/PlatformLogo";import {PLATFORMS} from "../lib/constants";import {overallStatus,formatDate,formatTime} from "../lib/helpers";export default function PublishCenter(){const {videos,updateItem}=useApp();const [tab,setTab]=useState("queue");const queue=[...videos].filter(v=>overallStatus(v)!=="Published").sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time));const published=videos.filter(v=>overallStatus(v)==="Published");async function markPublished(video){const platformStatus={};Object.keys(video.platformStatus||{}).forEach(k=>platformStatus[k]="Published");await updateItem("videos",video.id,{...video,platformStatus,stage:"Published"})}return <div className="page"><div className="page-head"><div><span className="eyebrow">Manual publish workflow</span><h2>Publish Center</h2></div></div><section className="panel publish-warning"><AlertTriangle size={20}/><div><b>Auto posting API off</b><p>এই module manual workflow হিসেবে functional। API approval হলে এখানেই auto-publish connect হবে।</p></div></section><div className="publish-tabs">{["queue","connections","history","checklist"].map(x=><button key={x} className={tab===x?"active":""} onClick={()=>setTab(x)}>{x}</button>)}</div>{tab==="queue"&&<section className="panel"><h3>Publishing Queue</h3>{queue.length?queue.map(v=><article className="publish-card" key={v.id}><div><h4>{v.title}</h4><p>{formatDate(v.date)} • {formatTime(v.time)} • {v.type==="full"?"Full Video":"Short Video"}</p><div className="logo-row">{Object.keys(v.platformStatus||{}).map(k=><PlatformLogo key={k} platform={k} title={v.platformStatus[k]}/>)}</div></div><div className="publish-actions"><button onClick={()=>markPublished(v)}><CheckCircle2 size={16}/> Mark Published</button></div></article>):<div className="empty">No pending publish queue</div>}</section>}{tab==="connections"&&<section className="panel"><h3>Platform Connections</h3><div className="connection-grid">{Object.entries(PLATFORMS).map(([k,p])=><article className="connection-card" key={k}><PlatformLogo platform={k}/><h4>{p.name}</h4><span className="status-soon">Manual Mode</span><p>OAuth/API integration slot ready.</p><button><ExternalLink size={15}/> Setup Later</button></article>)}</div></section>}{tab==="history"&&<section className="panel"><h3>Publish History</h3>{published.length?published.map(v=><article className="publish-card" key={v.id}><div><h4>{v.title}</h4><p>{formatDate(v.date)} • {formatTime(v.time)}</p></div><span className="history-done">Published</span></article>):<div className="empty">No published history yet</div>}</section>}{tab==="checklist"&&<section className="panel"><h3>Publish Checklist</h3><div className="checklist-list">{["Video file ready","Thumbnail ready","Caption ready","Hashtags ready","Platform selected","Publish time confirmed","Manual upload completed","Status updated"].map(x=><label key={x}><input type="checkbox"/> {x}</label>)}</div></section>}</div>}