import React from "react";import {useApp} from "../context/AppContext";import {overallStatus} from "../lib/helpers";export default function Workspace(){const {allVideos,brands,allMedia,tasks,notifications}=useApp();return <div className="page"><div className="page-head"><div><span className="eyebrow">Executive overview</span><h2>Workspace</h2></div></div><section className="workspace-grid">{[["Brands",brands.length,"Active content workspaces"],["Videos",allVideos.length,`${allVideos.filter(v=>overallStatus(v)==="Published").length} published`],["Assets",allMedia.length,"Stored media references"],["Tasks",tasks.length,"Campaign and team work"],["Reminders",notifications.length,"Notification center"],["Health","Good","System running"]].map(([a,b,c])=><div className="workspace-card" key={a}><span>{a}</span><strong>{b}</strong><p>{c}</p></div>)}</section></div>}